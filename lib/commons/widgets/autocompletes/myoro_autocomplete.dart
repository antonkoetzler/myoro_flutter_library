import 'package:flutter/material.dart';

/// An autocomplete.
final class MyoroAutocomplete extends StatelessWidget {
  const MyoroAutocomplete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Hello');
  }
}
