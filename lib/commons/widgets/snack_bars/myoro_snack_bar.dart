import 'package:flutter/material.dart';

/// Base snack bar.
final class MyoroSnackBar extends StatelessWidget {
  const MyoroSnackBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Start');
  }
}
